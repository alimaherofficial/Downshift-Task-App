/// A class that holds the string constants for named routes in the app.
class Routes {
  /// Initial route that leads to the splash screen.
  static const String initialRoute = '/';

  /// Route that leads to the tasks screen in the app.
  static const String tasksRoute = '/tasks';

  /// Route that leads to the add task screen in the app.
  static const String addTaskRoute = '/add-task';

  /// Route that leads to the edit task screen in the app.
  static const String editTaskRoute = '/edit-task';
}
